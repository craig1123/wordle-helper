{
  "name": "wordle-solver",
  "description": "An Alogrithm that thinks for you to help you solve wordle",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "wordle": "node wordle.js",
    "start": "rimraf dist && parcel src/index.html",
    "build": "rimraf docs && rimraf dist && parcel build src/index.html --dist-dir docs"
  },
  "engines": {
    "node": ">=14"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.2.1",
    "@parcel/transformer-webmanifest": "^2.2.1",
    "parcel": "^2.2.1",
    "rimraf": "^3.0.2"
  }
}
